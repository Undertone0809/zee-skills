interface:
  display_name: "Flomo Local API"
  short_description: "Query, export, create, and edit flomo memos via local auth"
  default_prompt: "Use $flomo-local-api to query, summarize, export, create, or edit my flomo memos through local/API data."
